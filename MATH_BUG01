/**
 * @input A : Integer
 * 
 * @Output Integer
 */
int isPrime(int A) {
	//int upperLimit = (int)(sqrt(A));
	if(A==1)
	return 0;
	int i;
	for (i = 2; i*i <= A; i++) {
		if (A % i == 0) return 0;
	}
	return 1;
}
